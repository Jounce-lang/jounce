fn main() {
    let numbers = vec![1, 2, 3, 4, 5];

    // Test 1: Closure with || as prefix
    let result = numbers.map(|| 42);

    // Test 2: Closure with no params
    let make_closure = || {
        return 10;
    };

    let value = make_closure();
}
