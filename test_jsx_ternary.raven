fn test() {
    let in_stock = true;
    let stock = 5;

    let result = in_stock ?
        <span class="in-stock">{stock} in stock</span> :
        <span class="out-of-stock">Out of stock</span>;

    return result;
}
