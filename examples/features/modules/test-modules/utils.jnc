// Utility functions to test imports

fn double(n: i64) -> i64 {
    return n * 2;
}

fn format_number(n: i64) -> String {
    return "Number: " + n.to_string();
}

struct Point {
    x: i64,
    y: i64,
}

const PI: f64 = 3.14159;
