// App 25: Object literals in JSX
component App() {
    let user = createSignal({ name: "Alice", age: 30 });

    <div>
        <p>Name: {user.value.name}</p>
        <p>Age: {user.value.age}</p>
    </div>
}
