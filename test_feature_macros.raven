// Test: Macro invocations
fn main() {
    // vec! macro
    let numbers = vec![1, 2, 3, 4, 5];

    // println! macro
    println!("Hello, World!");

    // format! macro
    let name = "Alice";
    let greeting = format!("Hello, {}!", name);

    // panic! macro (commented out)
    // panic!("This would crash");

    return 0;
}
