struct Item {
    id: i32,
}

struct Data {
    items: Vec<Item>,
}

fn test() {
    let data = Data {
        items: vec![],
    };
    let mut result = vec![];

    for item in data.items {
        if item.id != 5 {
            result.push(item);
        }
    }
}
