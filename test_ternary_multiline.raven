fn test() {
    let in_stock = true;
    let stock = 5;

    // Multiline ternary with indentation
    return <div>
        {in_stock ?
            <span>{stock} in stock</span> :
            <span>Out</span>
        }
    </div>;
}
