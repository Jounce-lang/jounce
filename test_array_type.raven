fn find_first_positive(numbers: [i32; 5]) -> i32 {
    for num in numbers {
        if num > 0 {
            return num;
        }
    }
    -1
}

fn main() {
    let nums = [1, 2, 3, 4, 5];
    let result = find_first_positive(nums);
}
