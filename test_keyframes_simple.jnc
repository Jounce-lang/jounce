fn main() {
    let styles = css! {
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .button {
            background: blue;
            animation: fadeIn 0.3s ease-in;
        }
    };
    println!("Button with animation");
}
