component Header() {
    let item_count = 5;

    return <header class="header">
        <div class="container">
            <h1>ShopOne</h1>
            <nav class="nav">
                <span>Cart</span>
                {item_count > 0 && (
                    <span class="cart-badge">{item_count}</span>
                )}
            </nav>
        </div>
    </header>;
}

fn main() {
    let x = Header();
}
