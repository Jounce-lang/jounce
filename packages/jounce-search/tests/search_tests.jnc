use jounce_search::SearchIndex;
#[test]
fn test_search_index() { let mut idx = SearchIndex::new(); idx = idx.add_document("1", "hello world"); let results = idx.search("hello"); assert_eq!(results.len(), 1); }
