struct CartItem {
    product_id: i32,
    quantity: i32,
}

struct CartState {
    items: Vec<CartItem>,
    total: i32,
}

fn test() {
    let mut items = vec![];

    for item in vec![] {
        if item.product_id != 0 {
            items.push(item);
        }
    }
}
