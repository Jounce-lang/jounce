component Test() {
    let condition = true;
    <div>
        {condition ? {
            let x = 5;
            <p>Value: {x}</p>
        } : {
            let y = 10;
            <p>Other: {y}</p>
        }}
    </div>
}
