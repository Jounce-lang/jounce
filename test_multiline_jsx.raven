struct Product {
    stock: i32
}

fn test() {
    let product = Product { stock: 5 };

    // Same structure but without ternary
    return <div class="stock">
        <span class="in-stock">{product.stock} in stock</span>
    </div>;
}
