// Test event handlers in JSX

component Counter() {
    let count = 0;

    return <div>
        <h1>Counter: {count}</h1>
        <button onClick={() => count + 1}>
            Increment
        </button>
    </div>;
}

fn main() {
    let app = Counter();
}
