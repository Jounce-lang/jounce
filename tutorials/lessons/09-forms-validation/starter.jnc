// Lesson 9: Forms & Validation
// Build interactive forms with two-way binding

component App() {
    let name = createSignal("");
    let email = createSignal("");

    let handleSubmit = () => {
        console.log("Name:", name.value);
        console.log("Email:", email.value);
    };

    <form onSubmit={(e) => { e.preventDefault(); handleSubmit(); }} class="container p-8 max-w-md">
        <h1 class="text-3xl font-bold mb-6">Sign Up Form</h1>

        <input
            type="text"
            placeholder="Your Name"
            class="w-full mb-4 p-3 border rounded"
            value={name.value}
            onInput={(e) => name.set(e.target.value)}
        />

        <!-- TODO: Add email input (copy pattern from name input) -->


        <button type="submit" class="btn btn-primary w-full">
            Submit
        </button>
    </form>
}
