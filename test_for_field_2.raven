struct Data {
    items: Vec<i32>,
}

fn test() {
    let data = Data {
        items: vec![1, 2, 3],
    };
    let mut result = vec![];

    for item in data.items {
        result.push(item);
    }
}
