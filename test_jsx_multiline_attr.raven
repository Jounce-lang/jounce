fn test() {
    let quantity = Signal::new(5);

    <button
        onclick={() => {
            if quantity.get() > 1 {
                quantity.set(quantity.get() - 1);
            }
        }}
        disabled={quantity.get() <= 1}
    >
        "-"
    </button>;
}
