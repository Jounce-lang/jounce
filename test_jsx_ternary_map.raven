component App() {
    let loading = false;
    let items = vec![1];

    return <div>
        {loading ? (
            <div>Loading</div>
        ) : (
            <div>
                {items.iter().map(|x| {
                    <span>test</span>
                })}
            </div>
        )}
    </div>;
}
