// Test: JSX with special characters
fn main() {
    // @ symbol in JSX text
    let username = <p>User: @alice</p>;

    // Ellipsis in JSX text
    let loading = <p>Loading</p>;

    // Emoji (should fail based on docs)
    // let emoji = <p>⭐</p>;

    return 0;
}
